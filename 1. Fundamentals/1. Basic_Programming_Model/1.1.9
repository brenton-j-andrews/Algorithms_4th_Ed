public class one_one_nine {
    public static void main(String[] args) {
        int input = Integer.parseInt(args[0]);
        String output = "";
        for (int n = input; n > 0; n = n / 2) {
            output = (n % 2) + output;
        }

        System.out.println(output);
    }
}
